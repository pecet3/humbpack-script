let names = ["Jakub", "Wiktor", "Aleczek"];

let i = len(names);
let x = -1;


loop(
  fn() { let x = x + 1; x < i; },
  fn() { 
    if (names[x] == "Aleczek"){
      print("Gwiazda Twitcha: ")
    }
    print(names[x]);
  }
);

