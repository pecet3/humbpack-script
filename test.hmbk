
mut math = {
  "number": 4,
  "add_to_number" : fn(x){
    return math["number"] + 2;
  }
}

print(math["add_to_number"](2));

const z = 454;
const add = fn(x){
  const z = 10;
  return z + x;
}

print(add(4))